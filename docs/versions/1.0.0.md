# Thumbi 1.0.0

작성일: 2026-04-11  
앱 버전: `1.0.0`

## 목적

`Thumbi`는 아이의 잘한 일과 아쉬운 일을 빠르게 기록하고, 날짜별 / 기간별 흐름을 로컬에서 확인하기 위한 개인용 앱이다.

## 현재 제품 범위

### 1. 메인 탭: 업다운

- 메인 탭 이름은 `업다운`
- 핵심 행동은 `엄지 업`, `엄지 다운` 입력
- 메모 입력 후 기록 가능
- 오늘 점수와 오늘의 상승 / 하락 건수 표시
- 최근 기록 목록 표시
- 최근 입력 직후 하단 `undo snackbar`로 되돌리기 가능

### 2. 달력 탭

- 현재 월 기준 `월간 달력` 제공
- 날짜 셀에서 일별 점수와 건수 확인 가능
- 날짜 선택 시 `일별 상세 모달` 표시
- 상세 모달에서:
  - 해당 날짜 점수
  - 상승 / 하락 건수
  - 해당 날짜 기록 목록
  - 해당 날짜 전체 기록 초기화

### 3. 통계 탭

- 현재 기간 집계:
  - 이번 주
  - 이번 달
  - 이번 분기
- 최고 기록:
  - 월간 최고 상승 / 최고 하락
  - 분기 최고 상승 / 최고 하락
  - 연간 최고 상승 / 최고 하락

## UX 규칙

### 입력

- 탭: 기록 1건 즉시 추가
- 롱프레스: 즉시 `5건` 입력 후, 누르고 있는 동안 `1초마다 5건` 추가
- 롱프레스 묶음 입력도 `undo snackbar`에서 한 번에 취소

### 되돌리기

- `undo snackbar`는 마지막 추가 행동 1개만 추적
- 단건 입력이면 1건 취소
- 롱프레스 반복 입력이면 해당 배치 전체 취소
- 표시 시간은 약 `5초`

### 정보 제공

- 메인 화면 카피는 최소화
- 상세 기능 설명은 상단 `Thumbi` 이름 옆 `i` 아이콘 모달로 이동

### 내비게이션

- 하단 탭 3개:
  - `업다운`
  - `달력`
  - `통계`

## 시각 언어

- 상승은 `빨간색`
- 하락은 `파란색`
- 이 색 문법을 메인 버튼, 통계 카드, 최근 기록, 아이콘 배지까지 통일
- 아이콘은 `맥 아이콘 느낌`의 부드러운 유리질감 + 카드 레이어 구조

## 기술 구현

### 클라이언트

- `Expo`
- `React Native`
- `TypeScript`

### 저장

- 백엔드 없음
- 기기 로컬 저장만 사용
- 저장소는 `@react-native-async-storage/async-storage`
- 기록 데이터 키: `@thumbi/vote-entries`

### 주요 파일

- 메인 앱 화면: [App.tsx](/Users/infini/workspace/thumbi/App.tsx:1)
- 기록 / 집계 로직: [src/lib/records.ts](/Users/infini/workspace/thumbi/src/lib/records.ts:1)
- 색상 / 폰트 토큰: [src/theme.ts](/Users/infini/workspace/thumbi/src/theme.ts:1)
- 앱 설정: [app.json](/Users/infini/workspace/thumbi/app.json:1)
- 아이콘 원본:
  - [assets/source/icon.svg](/Users/infini/workspace/thumbi/assets/source/icon.svg:1)
  - [assets/source/adaptive-icon.svg](/Users/infini/workspace/thumbi/assets/source/adaptive-icon.svg:1)
  - [assets/source/splash-art.svg](/Users/infini/workspace/thumbi/assets/source/splash-art.svg:1)
- 아이콘 생성 스크립트: [scripts/generate-assets.sh](/Users/infini/workspace/thumbi/scripts/generate-assets.sh:1)

## Android 설치 상태

- Android 패키지명: `com.infini.thumbi`
- 네이티브 Android 프로젝트 생성 완료
- 독립 실행용 릴리즈 APK 빌드 가능
- 릴리즈 APK 산출 경로:
  - `/Users/infini/workspace/thumbi/android/app/build/outputs/apk/release/app-release.apk`

## 구현 히스토리 요약

아래 커밋 흐름으로 1.0.0 기능이 완성됐다.

- `d26362c` Build initial Thumbi MVP
- `5d79de4` Add Thumbi app icon assets
- `ac51eee` Refine Thumbi icon direction
- `f9c9557` Shift icon badges to stock red blue
- `c8afa41` Align UI and icon with red blue trend cues
- `fbe9a0a` Add quarter and record-high stats
- `ce78c65` Add undo snackbar for new entries
- `6d12cfa` Add calendar reset and repeat input
- `b7611ea` Split UX into bottom tabs
- `93bdfb0` Configure native Android install
- `dbf6bd8` Trim copy and move help into info modal

## 아직 없는 것

- 사용자 계정 / 클라우드 동기화
- 다중 프로필
- 설정 화면
- 배포용 전용 release keystore 관리 문서
- Play Store 배포 준비

## 이후 버전 문서 작성 규칙

새 버전을 만들 때는 다음을 같이 갱신한다.

1. `package.json` 버전
2. `CHANGELOG.md`
3. `docs/versions/<version>.md`

새 버전 문서에는 최소한 아래를 포함한다.

- 추가된 기능
- 변경된 UX
- 삭제된 기능
- 저장 구조 변경 여부
- Android/iOS 빌드 방식 변경 여부
- 관련 핵심 커밋
